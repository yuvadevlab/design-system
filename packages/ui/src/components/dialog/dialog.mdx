import { Meta, Title, Description, Canvas, Controls } from "@storybook/blocks";
import * as DialogStories from "./dialog.stories";

<Meta of={DialogStories} />

<Title />

# Dialog

Modal dialog built on accessible Radix primitives with focus trapping, ESC dismiss, and animated backdrop overlays.

## Default

<Canvas of={DialogStories.Default} />

## Component Properties

<Controls />

{
  "name": "yuva-devlab-design-system",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build --filter=!//",
    "build:packages": "turbo run build --filter=./packages/**",
    "dev": "turbo run dev --filter=!//",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "turbo run type-check --filter=!//",
    "typecheck": "pnpm run type-check",
    "test": "turbo run test --filter=!//",
    "test:coverage": "turbo run test --filter=!// -- --coverage",
    "clean": "turbo run clean --filter=!//",
    "validate": "pnpm run format:check && pnpm run lint && pnpm run type-check && pnpm run build && pnpm run test",
    "fix": "pnpm run format && pnpm run lint:fix",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish",
    "prepare": "husky"
  },
  "packageManager": "pnpm@11.13.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@eslint/js": "^9.39.1",
    "@yuva-devlab/cli": "workspace:*",
    "eslint": "^9.39.1",
    "eslint-plugin-prettier": "^5.5.6",
    "eslint-plugin-tailwindcss": "^4.4.0",
    "globals": "^16.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.9.8",
    "tailwindcss": "^4.3.3",
    "turbo": "^2.11.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.47.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix --no-warn-ignored --max-warnings=0",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml,css,scss}": [
      "prettier --write"
    ]
  }
}
